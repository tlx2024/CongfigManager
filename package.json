{
    "name": "configreader-config-manager",
    "private": true,
    "license": "AGPL-3.0-or-later",
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "dev": "tauri dev",
        "build": "tauri build",
        "check": "npm --prefix frontend run typecheck && npm --prefix frontend run test && npm --prefix frontend run build"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2.0.0"
    }
}
