在下方 **Assets** 中选择适合系统的安装包：

- **Windows x64**：`.exe` 安装程序或 `.msi`。
- **macOS**：Apple Silicon 与 Intel 分别下载对应的 `.dmg`。
- **Linux x64**：通用 `.AppImage` 或 Debian/Ubuntu 使用的 `.deb`。

安装后打开 ConfigManager，应用会准备示例配置；也可以选择自己的 JSON/XML 配置目录。

macOS 安装包使用临时签名，未进行 Apple 公证；首次打开时可能需要在系统隐私与安全设置中允许。
